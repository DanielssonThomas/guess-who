interface GameGuessProps {
  guessText: string;
  guessDBname: string;
  guessed: boolean;
  key: number;
}

const GameGuess = ({
  guessText,
  guessDBname,
  guessed,
  key,
}: GameGuessProps) => {
  return <div></div>;
};

export default GameGuess;
